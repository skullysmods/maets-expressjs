meta {
  name: Post Task
  type: http
  seq: 2
}

post {
  url: {{server}}:{{port_base}}/tasks
  body: json
  auth: inherit
}

body:json {
  {
    "title" : "My Task",
    "done" : false
  }
}

settings {
  encodeUrl: true
}
