meta {
  name: Delete
  type: http
  seq: 5
}

delete {
  url: {{server}}:{{port_base}}/games/1
  body: json
  auth: inherit
}

body:json {
  {
    "done" : true
  }
}

settings {
  encodeUrl: true
}
