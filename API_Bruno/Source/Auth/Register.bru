meta {
  name: Register
  type: http
  seq: 3
}

post {
  url: {{server}}:{{port_base}}/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "email" : "Hello@hWorld.com",
    "password" : "password"
  }
}

settings {
  encodeUrl: true
}
